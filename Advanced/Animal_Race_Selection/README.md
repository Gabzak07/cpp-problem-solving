# Animal_Race_Selection

Filters top-performing contenders from multiple qualifying heats and ranks the finalists by completion time.

Input: `Duomenys.txt` — number of groups, participant counts, fixed-width names, and split times (minutes and seconds)

Output: `Rezultatas.txt` — qualified participants sorted in ascending order by race time