# City_Country_Population_Summary

Groups city records by country and calculates each country’s city count and total population.

Input: `duom.csv` — city name, country name, and population records  
Output: `rez.txt` — countries with city counts, total population, and listed cities