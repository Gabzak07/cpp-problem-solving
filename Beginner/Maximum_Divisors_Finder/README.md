# Maximum_Divisors_Finder

Finds the integer within a specified range that has the highest number of divisors.

Input: `Duomenys.txt` — two integers representing the start and end of the range.  
Output: `Rezultatas.txt` — the number with the most divisors, its total divisor count, and a full list of those divisors.