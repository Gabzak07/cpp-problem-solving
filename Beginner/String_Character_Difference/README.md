# String_Character_Difference

Compares two character strings and identifies which characters from the first string are missing in the second string.

Input: `Duomenys.txt` — two separate strings of characters.  
Output: `Rezultatas.txt` — a list of characters present only in the first string, or a confirmation message if all are found in the second.