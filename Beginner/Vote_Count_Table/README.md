# Vote_Count_Table

Counts votes for each candidate and generates a simple voting results table.

Input: `Duomenys.txt` — number of voters, number of candidates, and each voter’s selected candidate  
Output: `Rezultatas.txt` — vote count table for all candidates