# Wintering_Birds_Sorting

Reads the population counts of three wintering bird species, sorts them in descending order, and calculates the difference between the largest and smallest populations.

Input: `Duomenys.txt` — three integers representing the bird counts.  
Output: `Rezultatas.txt` — the sorted counts from highest to lowest, followed by the calculated difference between the maximum and minimum values.