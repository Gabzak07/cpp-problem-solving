# Array_Value_Insertion

Searches a numerical sequence for a specific target value to insert a duplicate at its position, or appends the target to the end if it does not exist in the sequence.

Input: `Duomenys.txt` — sequence length, the target number, and the initial sequence of integers.  
Output: `Rezultatas.txt` — the updated sequence containing the newly inserted number.