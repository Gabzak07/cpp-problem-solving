# Prime_Multiple_Insertion

Inserts composite numbers from a secondary sequence into a sorted sequence of prime numbers immediately following each prime factor they are divisible by.

Input: `Duomenys.txt` — length and elements of prime sequence A, followed by length and elements of integer sequence B.  
Output: `Rezultatas.txt` — the modified sequence A containing the inserted multiples.